/** Shared mines grid — safe for client bundles (no Node imports). */

export const WEB_MINES_TOTAL = 16;
export const WEB_MINES_COUNT = 5;
export const WEB_MINES_SAFE = WEB_MINES_TOTAL - WEB_MINES_COUNT;
